{
  "name": "lookup-arrays",
  "version": "1.0.0",
  "description": "A method of centralizing data that can be accessed by arrays with less memory comsumption than copying data",
  "main": "index.js",
  "scripts": {
    "test": "jest --config jestconfig.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jest": "^27.5.1",
    "ts-jest": "^27.1.3"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0"
  }
}
